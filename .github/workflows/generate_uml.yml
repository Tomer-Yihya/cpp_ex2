name: Generate UML Diagrams

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  generate-diagrams:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install PlantUML
      run: sudo apt-get update && sudo apt-get install -y plantuml

    - name: List files before generating diagrams
      run: ls -la

    - name: List all cpp files
      run: find . -name '*.cpp'

    - name: Generate UML Diagrams
      run: |
        mkdir -p diagrams
        find . -name '*.cpp' -exec sh -c 'echo Processing $1 && plantuml -tpng $1 && mv ${1%.cpp}.png diagrams/' _ {} \;
        ls -la diagrams

    - name: List files after generating diagrams
      run: ls -la diagrams

    - name: Upload UML Diagrams
      uses: actions/upload-artifact@v2
      with:
        name: UML Diagrams
        path: diagrams
